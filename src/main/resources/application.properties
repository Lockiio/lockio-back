server.port=8080
server.address=localhost

# MySQL configuration
spring.datasource.url=jdbc:mysql://eu-cdbr-west-03.cleardb.net/heroku_7bf498755bc3e50
spring.datasource.user=${USER}
spring.datasource.password=${PASSWORD}

spring.jpa.database=mysql
spring.jpa.hibernate.ddl-auto=create-drop

# Parameters used for debugging
spring.logging.level.org.hibernate.SQL=debug
spring.jpa.show-sql=true

# Swagger address for api doc : http://localhost:8080/swagger-ui/index.html#/